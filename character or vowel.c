#include<stdio.h>
#include<conio.h>
#include<math.h>
int main()
{
    clrscr();
    char ch;
    printf("enter the character \n");
    scanf("%c",&ch);
    if(ch=='a')
    {
    printf("the character is vowel");
    }
    else if (ch=='e')
    {
    printf("the character is vowel");
    }
    else if (ch=='i')
   { printf("the character is vowel");
   }
   else if (ch=='o')
   {
   printf("the character is vowel");
   }
   else if (ch=='u')
   {
   printf("the character is vowel");
   }
   
    else
    {
    printf("the character is constant");
    }
    
    getch();
    
}
